<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.controlspractice">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ControlsPractice">
        <activity android:name=".seekbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ratingbar">

        </activity>
        <activity android:name=".webview"></activity>
        <activity android:name=".searchview" />
        <activity android:name=".SharedPreferences2" />
        <activity android:name=".SharedPreferences1" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>